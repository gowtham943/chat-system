# Keeps a hot instance of Gradle in the background so it doesn't have to restart every time
org.gradle.daemon=true
# Uses all available CPU cores to compile your Java files in parallel
org.gradle.parallel=true
# Caches task outputs to skip compiling unchanged code
org.gradle.caching=true